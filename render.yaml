services:
  - type: web
    name: donkey-uno-server
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3001
    healthCheckPath: /health
    autoDeploy: true
